
<header>

</header>

<main class="flex flex-col px-5 py-10 gap-5 items-center">
<h1 class="text-6xl text-gray-900">Tokenize the World</h1>
<h2 class="text-2xl text-gray-800">The metaverse is the next evolution of social connection</h2>
<div class="flex flex-wrap w-1/2 gap-10 justify-center pt-16">
  <% @articles.each_with_index do |article, index| %>
    <%= link_to article_path(article) do %>
      <div class="flex flex-col w-80 gap-4 bg-white pt-1 shadow-lg hover:shadow-xl rounded-lg">
        <% image_path = "dummy#{index + 1}.jpg" %>
        <% image_path = "dummy1.jpg" unless File.exist?(Rails.root.join('app', 'assets', 'images', image_path)) %>
        <%= image_tag(image_path, class: "rounded-t-lg", alt: "Article") %>
        <strong class="font-medium text-xl px-5 mt-1"><%= article.title %></strong>
        <div class="flex justify-between px-5 mb-2">
          <p><%= article.author ? article.author : "Elon Musk" %></p>
          <small class="text-gray-600"><%= article.created_at.strftime("%c") %></small>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
<a href="/articles/new" class="m-5 px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600">New Article</a>
</main>
